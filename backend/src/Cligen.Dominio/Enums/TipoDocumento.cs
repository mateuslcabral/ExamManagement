namespace Cligen.Dominio.Enums;

/// <summary>Documento de identificação do paciente (Q8). O par (tipo, número) tem índice único.</summary>
public enum TipoDocumento
{
    Cpf = 1,
    Passaporte = 2
}
