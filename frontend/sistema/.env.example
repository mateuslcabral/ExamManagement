# Copie para .env.local e preencha.
AUTH_SECRET=<gere com: openssl rand -base64 32>
AUTH_TRUST_HOST=true
# URL da API Cligen (backend) e a mesma ChaveDeServico configurada lá
CLIGEN_API_URL=http://localhost:5053
CLIGEN_API_KEY=<ChaveDeServico do appsettings.Development.json do backend>
